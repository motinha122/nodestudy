"dog".charAt(1) // "o"
'YouTube'.substring(1,2) // 'o'
'YouTube'.substring(3,7) // 'Tube'

Substring return the characters between the specified indexes .substring(startIndex,endIndex)

'YouTube'.substring(1) // 'ouTube' 

returns all the characters from the specified value

Number.EPSILON returns the smallest interval between two numbers

let array1 = [1,2,3,4,5];
array1.splice(1,2,[5,6,7]); returns [2,3], array1 = [1,[5,6,7],4]